diff --git a/src/components/InquiryModal.tsx b/src/components/InquiryModal.tsx
--- a/src/components/InquiryModal.tsx
+++ b/src/components/InquiryModal.tsx
@@
   const [orderType, setOrderType] = useState<'commander' | 'sur_mesure' | 'essayage'>('commander');
+  const [salutation, setSalutation] = useState<'Monsieur' | 'Madame' | 'Autre'>('Monsieur');
   const [clientName, setClientName] = useState('');
@@
         body: JSON.stringify({
+          salutation,
           clientName: clientName || 'Client Anonyme',
@@
         clientName: clientName || 'Client Anonyme',
+        salutation,
         clientEmail: clientEmail || 'client@example.com',
@@
         clientName: clientName || 'Client Anonyme',
+        salutation,
         clientEmail: clientEmail || 'client@example.com',
@@
               <div className="space-y-4">
@@
                 <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">
+                  <div>
+                    <label className="block text-xs uppercase tracking-wider text-[#a3b1a5] mb-1 font-medium">
+                      Civilité
+                    </label>
+                    <select
+                      value={salutation}
+                      onChange={(e) => setSalutation(e.target.value as 'Monsieur' | 'Madame' | 'Autre')}
+                      className="w-full bg-[#1b231d] border border-[#38483b] text-[#f3ece0] text-xs rounded-xl p-3 focus:border-[#d4af37] outline-none cursor-pointer"
+                    >
+                      <option value="Monsieur">Monsieur</option>
+                      <option value="Madame">Madame</option>
+                      <option value="Autre">Autre</option>
+                    </select>
+                  </div>
                   <div>
                     <label className="block text-xs uppercase tracking-wider text-[#a3b1a5] mb-1 font-medium flex items-center space-x-1.5">
@@
 diff --git a/api/orders.ts b/api/orders.ts
@@
 import {
@@
 } from './_helpers.js';
+import { sendTemplatedOrderEmail } from './emailTemplates.js';
@@
       clientName,
+      salutation,
       clientEmail,
@@
       clientName:
         String(clientName).trim(),
+      salutation: String(salutation || 'Autre').trim(),
@@
-    const emailResult =
-      await sendOrderEmailNotification({
+    const emailResult =
+      await sendTemplatedOrderEmail({
@@
         clientName:
           createdOrder.clientName,
+        salutation:
+          createdOrder.salutation,
@@
       order: createdOrder,
@@
 diff --git a/src/utils/orderStorage.ts b/src/utils/orderStorage.ts
@@
   clientName: string;
+  salutation?: 'Monsieur' | 'Madame' | 'Autre';
@@
 export const createOrder = (orderData: {
@@
   clientName: string;
+  salutation?: 'Monsieur' | 'Madame' | 'Autre';
@@
     clientName: orderData.clientName,
+    salutation: orderData.salutation || 'Autre',
@@
 diff --git a/src/components/OrdersModal.tsx b/src/components/OrdersModal.tsx
@@
 import { OrdersManagementView } from './OrdersManagementView';
+import { EmailTemplatesEditor } from './EmailTemplatesEditor';
@@
-        <div className="flex-1 overflow-y-auto p-4 sm:p-6"><OrdersManagementView ordersEmail={ordersEmail} reportEmail={reportEmail} /></div>
+        <div className="flex-1 overflow-y-auto p-4 sm:p-6 space-y-6">
+          <EmailTemplatesEditor />
+          <OrdersManagementView ordersEmail={ordersEmail} reportEmail={reportEmail} />
+        </div>
